[project]
name = "mlserve-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "torch",
  "torchvision",
  "transformers",
  "ray[serve]"
]
